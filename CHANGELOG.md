# Changelog

## 3.0.17

- 11번가 비로그인 장바구니에서 `input.input_count_modify`의 실제 수량값을 우선 읽도록 개선
- `input[title="수량"]`, `input[name^="qty_"]`, 수량변경 버튼 값을 순차적으로 확인하는 보조 로직 추가
- 로그인 장바구니에서 동작하던 기존 `쿠폰변경` 텍스트 기반 수량 추출은 fallback으로 유지
- 11번가 로그인/비로그인 화면 구조 차이를 모두 지원하도록 상품 컨테이너 탐색 보강
- 화면/추출기 버전을 `v3.0.17`로 동기화

## 3.0.16

- S2B 상품 추출을 화면 텍스트 추측 방식에서 체크박스 value의 JSON 데이터 기반으로 전환
- S2B 수량은 현재 수량 입력값을 우선 사용하고 JSON의 `estimate_quantity`를 보조값으로 사용
- S2B 단가는 `estimate_amt`, 규격은 `size`, 단위는 `credit_str`, 상품명은 `goods_name`에서 직접 추출
- 조건부무료/묶음배송을 자체 계산하지 않고 S2B가 최종 계산한 `tot_fee` 값을 배송비 한 줄로 반영
- `tot_fee`를 읽을 수 없는 예외에서만 배송비 `확인 필요` 처리
- 화면/추출기 버전을 `v3.0.16`으로 동기화

## 3.0.15

- S2B 장바구니 배송비 구조 확인용 `🧪 S2B 배송비 진단` 전용 페이지/북마클릿 추가
- 선택 상품 주변 텍스트, 탭 단위 값, 숫자 후보, 이미지(src/alt/title), input/button/link 정보와 조상 컨테이너를 로컬에서 수집해 복사할 수 있게 함
- 실제 S2B 추출 로직은 아직 변경하지 않음
- 화면/추출기 버전을 `v3.0.15`로 동기화

## 3.0.14

- G마켓 실제 상품 체크박스의 `.item` 영역만 처리하도록 추출 범위를 정리
- G마켓 `input.item_qty_count` 값을 직접 읽어 실제 수량을 반영
- G마켓 상품 총액을 수량으로 나눠 단가를 복원하고 나누어떨어지지 않는 경우 `확인 필요` 처리
- 기존 G마켓 배송비 형식 호환을 유지하면서 여러 배송비는 기존 합산 로직으로 한 줄 정리
- 11번가 수량 구조 확인용 `🧪 11번가 수량 진단` 전용 페이지/북마클릿 추가
- 화면/추출기 버전을 `v3.0.14`로 동기화

## 3.0.13

- G마켓 테스트가 쿠팡 진단 페이지와 헷갈리지 않도록 `gmarket-diagnose.html` 전용 진단 페이지 추가
- G마켓 진단 페이지에서 `🧪 G마켓 수량 진단` 북마클릿만 단독 제공
- 화면/추출기 버전을 `v3.0.13`으로 동기화

## 3.0.12

- G마켓 장바구니 수량 오인식 원인 확인을 위한 `🧪 G마켓 수량 진단` 북마클릿 추가
- 진단 페이지를 쿠팡/G마켓 공용 테스트 화면으로 정리
- G마켓 선택 상품 주변의 input/button/select 값, 수량 후보 요소, 조상 컨테이너 텍스트를 로컬에서 수집해 복사할 수 있게 함
- 실제 G마켓 추출 로직은 아직 변경하지 않음
- 화면/추출기 버전을 `v3.0.12`로 동기화

## 3.0.11

- 쿠팡 장바구니의 `.cart-quantity-input` 값을 직접 읽어 실제 수량을 반영
- 쿠팡 행의 표시 총액을 실제 수량으로 나눠 단가를 복원하도록 개선
- 총액이 수량으로 정확히 나누어지지 않는 예외는 `확인 필요`로 표시
- 품목별 배송비가 여러 줄 추출되면 금액을 합산해 `배송비` 한 줄로 정리하고 규격에 합산 건수를 표시
- 화면/추출기 버전을 `v3.0.11`로 동기화

## 3.0.10

- 쿠팡 장바구니 수량 오인식 원인 확인을 위한 별도 진단 페이지/북마클릿 추가
- 진단 도구는 선택 상품 영역의 텍스트, 금액, input/select/button 값과 수량 후보 요소를 로컬에서 수집해 복사할 수 있게 표시
- 실제 장바구니 추출 로직은 아직 변경하지 않음
- 화면/추출기 버전을 `v3.0.10`으로 동기화

## 3.0.9

- 추출할 상품이 없을 때 메인 화면을 새로 열던 동작 제거
- 현재 쇼핑몰 화면에서 안내 토스트만 약 2.6초 표시한 뒤 자동으로 숨기도록 변경
- 화면/추출기 버전을 `v3.0.9`로 동기화

## 3.0.8

- 크롬 북마크바에서 더 깔끔하게 보이도록 북마클릿 이름을 `🛒 딸깍 장바구니`로 간소화
- 화면/추출기 버전을 `v3.0.8`로 동기화

## 3.0.7

- 북마클릿 실행 시 추출할 상품이 없어도 딸깍 장바구니품의서 메인 화면을 열도록 개선
- 빈 추출 상태로 들어오면 기존 임시작업을 덮어쓰지 않고 빈 화면으로 진입
- 메인 화면에서 `선택된 상품을 찾지 못했습니다` 안내 토스트를 잠시 표시한 뒤 자동으로 숨김
- 화면/추출기 버전을 `v3.0.7`로 동기화

## 3.0.6

- 첫 안정 배포 준비 완료
- 화면의 `beta` 표기 제거
- 화면 버전과 장바구니 추출기 버전을 `v3.0.6`으로 확정

## 3.0.6-beta.1

- 작성 결과 영역의 중복 제목 `품의자료` 제거
- 행정 업무 표현에 맞춰 결과 영역 명칭을 `적요`로 정리
- 화면/추출기 버전을 `v3.0.6`으로 동기화

## 3.0.5-beta.1

- 장바구니 품목이 들어온 뒤에는 상단 1~4 단계 안내바를 자동으로 숨기도록 개선
- 처음 접속하거나 북마클릿 설치 전에는 단계 안내를 그대로 표시
- 화면/추출기 버전을 `v3.0.5`로 동기화

## 3.0.4-beta.1

- 품의개요 / 원인행위개요 탭을 더 크고 선명한 버튼형 탭으로 개선
- 기본 구입 목적을 초등학교에서 범용적으로 쓰기 좋은 `학생 교육활동`으로 변경
- 기존 기본값 `유아 교육활동`이 저장돼 있던 브라우저는 자동으로 새 기본값으로 전환
- 구입 목적의 마지막 글자 받침 여부에 따라 `을/를`을 자동 선택하도록 조사 처리 개선
- 화면/추출기 버전을 `v3.0.4`로 동기화

## 3.0.3-beta.1

- 화면 버전과 장바구니 추출기 버전을 동일한 릴리스 번호로 통일
- 이후 기능 수정 시 앱/추출기 버전을 함께 올리는 기준 적용

## 3.0.2-beta.1

- PC 화면을 2열 작업 레이아웃으로 재구성해 세로 스크롤을 줄임
- 왼쪽에 품목 확인·수정, 오른쪽에 구입 목적과 품의자료를 배치
- 품의개요/원인행위개요를 탭 방식으로 전환해 결과 영역을 압축
- 넓은 PC 화면에서는 콘텐츠 폭을 최대 1580px까지 활용
- 하단에 `개발자: 천안버들유치원 나대현` 표기 추가
- 화면 버전을 `v3.0.2 beta`로 상향

## 3.0.1-beta.1

- 화면 버전을 `v3.0.1 beta`로 상향하여 배포 코드 반영 여부를 바로 확인할 수 있게 함
- 추출 데이터에 추출기 버전을 포함하고 작업 영역에 `추출기 v3.0.1` 표시 추가
- 쿠팡 `품절임박 / 남은 상품 / 로켓배송 상품들` 안내 행을 상품명 단계와 최종 전달 단계에서 이중 차단

## 3.0.0-beta.1

- 쿠팡 그룹/품절임박 안내 영역이 상품으로 중복 추출되는 문제 수정
- 쿠팡 동일 상품 중복 추출 방지 키를 상품명·규격·단가 기준으로 강화

- 딸깍 여비정산서 기준으로 PC 글자 크기와 주요 UI 스케일 상향 조정

- 헤더 제목의 중복 `딸깍` 문구 제거 (`딸깍!` 모션 배지만 유지)

- 상단 제목에 `딸깍!` 브랜드 모션과 반짝임 효과 추가
- 통합 북마클릿 버튼에 눌림/호버 피드백 추가

- 헤더를 딸깍 아이콘과 어울리는 밝은 아이보리·오렌지 계열로 조정

- 딸깍 장바구니품의서 전용 아이콘을 헤더·favicon에 적용

- 딸깍 장바구니품의서 브랜드/UI 리빌딩 시작
- index.html 기반 GitHub Pages 구조 도입
- CSS/앱 로직 분리
- 통합 북마클릿을 현재 배포 주소 기준으로 자동 생성
- 추출 스크립트가 자신의 로드 주소를 기준으로 앱 URL을 계산하도록 변경
- 기존 cart.html을 새 메인 화면으로 연결하는 호환 페이지로 전환
- 품목 직접 추가/수정/삭제 및 금액 열 추가
- 품목 수와 배송비 합계를 분리 표시
- 구입 목적 입력값을 품의개요/원인행위개요에 자동 반영
- localStorage 임시저장/복구 추가
- showToast 공통 알림 구현
- S2B 조건부 배송비 3,000원 고정값 제거, 확인 필요 상태로 변경
- DOM 문자열 삽입 대신 안전한 요소 생성 방식으로 테이블 렌더링
